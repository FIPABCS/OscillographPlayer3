// 提示文件帮助 Visual Studio IDE 解释 Visual C++ 标识符,
// 如函数和宏的名称。
// 有关详细信息，请参见 https://go.microsoft.com/fwlink/?linkid=865984
#define HEAD EXTERNC __declspec(dllimport)
#define HEAD EXTERNC __declspec(dllexport)
#define CallingConvertion
#define EXTERNC
#define DefineSafeArray(T, typeName) typedef struct SafeArray { T* items; uint16_t width; uint16_t height; } SafeArray##typeName; static inline T Get##typeName(const SafeArray##typeName *array, int x, int y) { if (x < 0) { x = 0; } if (x >= array->width) { x = array->width - 1; } if (y < 0) { y = 0; } if (y >= array->height) { y = array->height - 1; } return array->items[y * array->width + x]; } static inline void Set##typeName(SafeArray##typeName *array, int x, int y, T value) { if (x < 0) { x = 0; } if (x >= array->width) { x = array->width - 1; } if (y < 0) { y = 0; } if (y >= array->height) { y = array->height - 1; } array->items[y * array->width + x] = value; return; }
